---
import MarkdownPageLayout from '../../layouts/MarkdownPageLayout.astro';
import { getEntry } from 'astro:content';

const entry = await getEntry('pages', 'smartmap');
const { Content } = await entry.render();
---

<MarkdownPageLayout title={entry.data.title} description={entry.data.description} lead="Smart Map">
  <Content />
</MarkdownPageLayout>
